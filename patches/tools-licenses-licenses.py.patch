diff --git a/tools/licenses/licenses.py b/tools/licenses/licenses.py
index 6b2eae031956dd3c69b2daf69ac6890078c701d4..f2770c2e31f37a47b22a91fb26028e99a7372c41 100755
--- a/tools/licenses/licenses.py
+++ b/tools/licenses/licenses.py
@@ -34,6 +34,7 @@ import find_depot_tools
 find_depot_tools.add_depot_tools_to_path()
 
 import metadata.fields.custom.license_allowlist as depot_tools_allowlist
+from brave_license_helper import AddBraveCredits, BRAVE_THIRD_PARTY_DIRS, CheckBraveMissingLicense, IsBraveRustCrate, ReportBraveIncompleteMetadataFile
 
 METADATA_FILE_NAMES = frozenset(
   {
@@ -375,6 +376,7 @@ SPECIAL_CASES = {
     "License File": ["//third_party/sample3/the_license"],
   },
 }
+(PRUNE_DIRS, ADDITIONAL_PATHS) = AddBraveCredits(_REPOSITORY_ROOT, PRUNE_PATHS, SPECIAL_CASES, PRUNE_DIRS, ADDITIONAL_PATHS)
 
 # These buildtools/third_party directories only contain
 # chromium build files. The actual third_party source files and their
@@ -877,7 +879,8 @@ def FindThirdPartyDirs(
         if skip in dirs:
           dirs.remove(skip)
 
-    if os.path.basename(path) == 'third_party':
+    if (os.path.basename(path) == 'third_party' or
+        os.path.basename(path) in BRAVE_THIRD_PARTY_DIRS or IsBraveRustCrate(path)):
       # Add all subdirectories that are not marked for skipping.
       for d in dirs:
         dirpath = os.path.join(path, d)
@@ -1081,6 +1084,7 @@ def _DiscoverMetadatas(args):
         m['dir'] = d
         metadatas.append(m)
     except LicenseError as lic_exp:
+      CheckBraveMissingLicense(d, lic_exp)
       had_errors = True
       # TODO(phajdan.jr): Convert to fatal error (https://crbug.com/39240).
       if args.enable_warnings:
@@ -1182,6 +1186,16 @@ def GenerateCredits(args, metadatas):
     },
     escape=False,
   )
+  # Growser-322: and ours, which MPL-2.0 3.2(a) asks us to point recipients
+  # at. On iOS this page is the only place the app can say it.
+  reciprocal_contents += EvaluateTemplate(
+    reciprocal_template,
+    {
+      'opensource_project': 'Growser',
+      'opensource_link': 'https://github.com/dsfox-idea/brave-core',
+    },
+    escape=False,
+  )
 
   template_contents = "<!-- Generated by licenses.py; do not edit. -->"
   template_contents += EvaluateTemplate(

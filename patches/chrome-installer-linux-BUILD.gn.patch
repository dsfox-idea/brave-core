diff --git a/chrome/installer/linux/BUILD.gn b/chrome/installer/linux/BUILD.gn
index b3b233d16abe096b16ae7a26202b28e925a44f91..89318a6b24e2a621146696f2bcb7cdda0631e0e0 100644
--- a/chrome/installer/linux/BUILD.gn
+++ b/chrome/installer/linux/BUILD.gn
@@ -18,6 +18,7 @@ import("//third_party/angle/gni/angle.gni")
 import("//third_party/widevine/cdm/widevine.gni")
 import("//ui/gl/features.gni")
 import("//ui/qt/qt.gni")
+import("//brave/build/config.gni")
 
 if (bundle_widevine_cdm) {
   import("//media/cdm/library_cdm/cdm_paths.gni")
@@ -103,6 +104,7 @@ if (build_with_internal_optimization_guide) {
   }
 }
 
+packaging_files_executables -= excluded_packaging_files_executables packaging_files_executables += brave_packaging_files_executables
 packaging_files_binaries = packaging_files_executables + packaging_files_shlibs
 
 # TODO(mmoss) Any convenient way to get all the relevant build files?
@@ -210,6 +212,7 @@ action("merge_rpm_dependencies") {
 strip_binary("strip_chrome_binary") {
   binary_input = "$root_out_dir/chrome"
   deps = [ "//chrome" ]
+  binary_input = "$root_out_dir/$brave_product_name" symbol_output = "$root_out_dir/$brave_product_name.debug" stripped_binary_output = "$root_out_dir/$brave_product_name.stripped" deps += strip_chrome_binary_deps  # Growser-197
 }
 
 strip_binary("strip_chrome_crashpad_handler") {
@@ -225,6 +228,7 @@ strip_binary("strip_chrome_management_service") {
 strip_binary("strip_chrome_sandbox") {
   binary_input = "$root_out_dir/chrome_sandbox"
   deps = [ "//sandbox/linux:chrome_sandbox" ]
+  binary_input = "$root_out_dir/${brave_product_name}_sandbox" symbol_output = "$root_out_dir/${brave_product_name}_sandbox.debug" stripped_binary_output = "$root_out_dir/${brave_product_name}_sandbox.stripped" deps += strip_chrome_sandbox_deps  # Growser-197
 }
 
 # On ChromeOS, system services depend on real ANGLE libraries rather than dummy
@@ -378,6 +382,7 @@ copy("common_packaging_files") {
   }
 
   outputs = [ "$root_out_dir/installer/common/{{source_file_part}}" ]
+  sources -= common_packaging_files_excluded_sources sources += common_packaging_files_sources
 }
 
 copy("deb_packaging_files") {
@@ -397,15 +402,36 @@ copy("deb_packaging_files") {
 
 copy("theme_files") {
   visibility = [ ":*" ]
+
+  # Growser-146/162: the build people install keeps the release mark; every
+  # other build carries the same mark in RED, so two browsers in one launcher
+  # cannot be confused and a development build cannot be mistaken for the
+  # product. Windows does this in the .rc (#146) and macOS in bundle_data
+  # (#162); Linux had no red icon at all until this.
+  #
+  # A DIRECTORY is chosen rather than a file renamed, because this copy uses
+  # {{source_file_part}} and everything downstream - the postinstall's
+  # xdg-icon-resource loop, the .desktop Icon= line - expects the release
+  # names. Read from //brave, like the mac .icns: branding.js copies only some
+  # theme files into the Chromium tree, so a second copy there would go stale
+  # in silence. brand/make-logos.py writes them.
+  import("//brave/build/growser.gni")
+  _logo_dir = "$branding_dir/linux"
+  _logo_dir_100 = "$branding_dir_100/linux"
+  if (!growser_public_release && branding_path_component == "brave") {
+    _logo_dir = "//brave/app/theme/brave/linux/development"
+    _logo_dir_100 = _logo_dir
+  }
+
   sources = [
     "$branding_dir/BRANDING",
-    "$branding_dir/linux/product_logo_128.png",
-    "$branding_dir/linux/product_logo_24.png",
-    "$branding_dir/linux/product_logo_256.png",
-    "$branding_dir/linux/product_logo_48.png",
-    "$branding_dir/linux/product_logo_64.png",
-    "$branding_dir_100/linux/product_logo_16.png",
-    "$branding_dir_100/linux/product_logo_32.png",
+    "$_logo_dir/product_logo_128.png",
+    "$_logo_dir/product_logo_24.png",
+    "$_logo_dir/product_logo_256.png",
+    "$_logo_dir/product_logo_48.png",
+    "$_logo_dir/product_logo_64.png",
+    "$_logo_dir_100/product_logo_16.png",
+    "$_logo_dir_100/product_logo_32.png",
   ]
   if (is_chrome_branded) {
     sources += [
@@ -486,6 +512,7 @@ group("installer_deps") {
     "//components/privacy_sandbox/privacy_sandbox_attestations/preload:component",
     "//sandbox/linux:chrome_sandbox",
   ]
+  public_deps += brave_installer_deps
   if (bundle_widevine_cdm) {
     public_deps += [ "//third_party/widevine/cdm" ]
   }
@@ -553,11 +580,14 @@ group("installer_deps") {
 template("linux_package") {
   assert(defined(invoker.channel))
   channel = invoker.channel
+  if (channel == "unstable" ) {
+    channel = "dev"
+  }
 
   if (is_chrome_branded) {
     package = "google-chrome"
   } else {
-    package = "chromium-browser"
+    package = brave_linux_package_name chrome_version_full = brave_version
   }
 
   deb_target_name = "${target_name}_deb"
@@ -566,7 +596,7 @@ template("linux_package") {
     script = "debian/build.py"
 
     inputs = packaging_files
-    outputs = [ "$root_out_dir/${package}-${channel}_${chrome_version_full}-1_${deb_arch}.deb" ]
+    if (channel == "stable") { outputs = [ "$root_out_dir/${package}_${chrome_version_full}_${deb_arch}.deb" ] } else { outputs = [ "$root_out_dir/${package}-${channel}_${chrome_version_full}_${deb_arch}.deb" ] }
 
     args = [
       "-a",
@@ -603,7 +633,7 @@ template("linux_package") {
       script = "rpm/build.py"
 
       inputs = packaging_files
-      outputs = [ "$root_out_dir/${package}-${channel}-${chrome_version_full}-1.${rpm_arch}.rpm" ]
+      if (channel == "stable") { outputs = [ "$root_out_dir/${package}-${chrome_version_full}-1.${rpm_arch}.rpm" ] } else { outputs = [ "$root_out_dir/${package}-${channel}-${chrome_version_full}-1.${rpm_arch}.rpm" ] }
 
       args = [
         "-a",
@@ -651,3 +681,6 @@ linux_package("unstable") {
 linux_package("canary") {
   channel = "canary"
 }
+linux_package("nightly") {
+  channel = "nightly"
+}

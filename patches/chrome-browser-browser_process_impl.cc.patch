diff --git a/chrome/browser/browser_process_impl.cc b/chrome/browser/browser_process_impl.cc
index 681ad0d1e178926969cb88ddf53bb100ed6ba604..9213d654199e7ee77820236ae864670aaf06b4e9 100644
--- a/chrome/browser/browser_process_impl.cc
+++ b/chrome/browser/browser_process_impl.cc
@@ -1671,6 +1671,7 @@ void BrowserProcessImpl::PreMainMessageLoopRun() {
   os_crypt_async_ =
       std::make_unique<os_crypt_async::OSCryptAsync>(std::move(providers));
 
+#if !BUILDFLAG(IS_MAC)
   // Trigger async initialization of OSCrypt key providers.
   os_crypt_async_->GetInstance(base::BindOnce(
       [](base::TimeTicks start_time,
@@ -1679,6 +1680,7 @@ void BrowserProcessImpl::PreMainMessageLoopRun() {
                                 base::TimeTicks::Now() - start_time);
       },
       base::TimeTicks::Now()));
+#endif  // Growser-236: lazy on macOS, see the recipe
 
   // browser_policy_connector() is created very early because local_state()
   // needs policy to be initialized with the managed preference values.
@@ -1721,7 +1723,7 @@ void BrowserProcessImpl::PreMainMessageLoopRun() {
 
 // Create the global SodaInstaller instance.
 #if !BUILDFLAG(IS_ANDROID) && !BUILDFLAG(IS_CHROMEOS)
-  soda_installer_impl_ = std::make_unique<speech::SodaInstallerImpl>();
+  soda_installer_impl_ = speech::CreateBraveSodaInstaller();
 #endif  // !BUILDFLAG(IS_ANDROID) && !BUILDFLAG(IS_CHROMEOS)
 
 #if BUILDFLAG(IS_CHROMEOS)

diff --git a/chrome/app/chrome_crash_reporter_client.cc b/chrome/app/chrome_crash_reporter_client.cc
index f0c0ff0871ee92a9ae7f4c634372cf0a550321d0..99b72b9553354409dda8ea100c89fe98f34c97fb 100644
--- a/chrome/app/chrome_crash_reporter_client.cc
+++ b/chrome/app/chrome_crash_reporter_client.cc
@@ -169,7 +169,7 @@ bool ChromeCrashReporterClient::IsRunningUnattended() {
 }
 
 bool ChromeCrashReporterClient::GetCollectStatsConsent() {
-#if BUILDFLAG(GOOGLE_CHROME_BRANDING)
+#if BUILDFLAG(GOOGLE_CHROME_BRANDING) || defined(OFFICIAL_BUILD)
   bool is_official_chrome_build = true;
 #else
   bool is_official_chrome_build = false;
@@ -196,9 +196,10 @@ bool ChromeCrashReporterClient::GetCollectStatsConsent() {
   return is_official_chrome_build;
 #else   // !BUILDFLAG(IS_ANDROID)
   if (!is_official_chrome_build) {
-    VLOG(1) << "GetCollectStatsConsent(): is_official_chrome_build is false "
-            << "so returning false";
-    return false;
+    // growser (#33): non-official builds upload Crashpad minidumps to our
+    // self-hosted GlitchTip. Consent is implied for our own product.
+    VLOG(1) << "GetCollectStatsConsent(): growser non-official -> true";
+    return true;
   }
   bool settings_consent = GoogleUpdateSettings::GetCollectStatsConsent();
   VLOG(1) << "GetCollectStatsConsent(): settings_consent: " << settings_consent
